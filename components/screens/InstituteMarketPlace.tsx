import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function InstituteMarketPlace() {
  return (
    <SafeAreaView>
      <Text>Institutes</Text>
    </SafeAreaView>
  );
}
